spring:
  application:
    name: retailer-service-dev

  datasource:
    url: jdbc:mysql://localhost:3306/retailer_db
    username: root
    password: note123@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

logging:
  level:
    root: DEBUG
    com.example: DEBUG
